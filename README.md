<div align="center">

# Zapretik

Умная консольная утилита (Python) для обхода DPI-блокировок **Discord** и **YouTube** в Windows.

Обёртка над [`winws.exe`](https://github.com/bol-van/zapret) — официальный репозиторий [bol-van/zapret](https://github.com/bol-van/zapret)

Разработано и поддерживается [miamura23](https://gitverse.ru/miamura23)

</div>

---

## ✨ Возможности

- **Два способа запуска.** Python-меню (`main.py`) для быстрого старта и **22 готовые bat-стратегии** (`general*.bat`) с пользовательскими списками — для Discord, YouTube и любых своих сайтов.
- **Автономность.** При первом запуске Zapretik сам проверяет наличие ядра (`winws.exe`, `WinDivert.dll`) и, если их нет, автоматически скачивает его с GitHub и распаковывает в рабочую папку.
- **Пользовательские списки.** Добавляйте свои домены в `lists\list-general-user.txt` — они обходят DPI (например, уже добавлены `crushon.ai` и `pulsesync.dev`).
- **Интерактивное меню.** 4 предустановленные стратегии обхода + пункт «Остановить сервис».
- **Права администратора.** Проверка прав через WinAPI и автоматический перезапуск с запросом UAC (winws.exe работает с драйвером WinDivert).
- **Логирование.** Весь вывод `winws.exe` пишется в `zapretik.log` и дублируется в консоль с цветовой маркировкой ошибок.
- **Корректная остановка.** По Ctrl+C или пункту меню процесс `winws.exe` завершается полностью — без зависших процессов и «застрявших» драйверов.
- **Автопубликация релизов.** Скрипт `release.py` сам обновляет версии, собирает архив `.rar` и загружает его в GitVerse-релиз.

---

> [!CAUTION]
>
> ### ФЕЙКИ
> Я не веду никакие другие страницы/группы в телеграме, ютуб-каналы или сайты.
> Если вы наткнулись на что-то вне этой страницы GitVerse, что распространяется от моего имени — это **фейк**.
> Единственный официальный репозиторий: https://gitverse.ru/miamura23/zapretik

> [!WARNING]
>
> ### АНТИВИРУСЫ
> `winws.exe` использует драйвер **WinDivert** — это инструмент для перехвата и фильтрации трафика, необходимый для работы zapret (замена `iptables`/`NFQUEUE` из Linux, которых нет под Windows). Он может использоваться как хорошими, так и плохими программами, но сам по себе вирусом не является. Драйвер `WinDivert64.sys` подписан для загрузки в 64-битное ядро Windows.
>
> Некоторые антивирусы относят файлы WinDivert к классам повышенного риска или хакерским инструментам и помещают их в карантин. Детект обязательно имеет название `WinDivert` или `Not-a-virus:RiskTool.Multi.WinDivert`.
>
> В случае проблем добавьте папку с Zapretik в исключения антивируса либо отключите детектирование PUA (потенциально нежелательных приложений). Например, в Kaspersky это галочка «Обнаруживать легальные приложения, которые злоумышленники часто используют для нанесения вреда». При аккуратной настройке исключений достаточно исключения; если не уверены — отключите детект PUA.

> [!IMPORTANT]
>
> ### ИСТОЧНИКИ БИНАРНИКОВ
> Все бинарные файлы ядра скачиваются автоматически с GitHub в таком порядке:
> 1. релиз [`bol-van/zapret`](https://github.com/bol-van/zapret/releases) (если в нём есть Windows-сборка);
> 2. проверенные Windows-сборки `winws.exe` от сообщества;
> 3. архив репозитория [`bol-van/zapret-win-bundle`](https://github.com/bol-van/zapret-win-bundle).
>
> Проверяйте, что запускаете: используйте сборки только из официальных источников.

---

## ⚙️ Использование

### 1. Включите Secure DNS
- **Chrome** — «Использовать безопасный DNS» и выбрать поставщика услуг DNS (любой, отличный от «Поставщик по умолчанию»).
- **Firefox** — «Включить DNS через HTTPS, используя: Максимальную защиту», затем «Выбрать поставщика» и вписать URL вручную, например `https://dns.google/dns-query` (Cloudflare может быть заблокирован).
- **Windows 11** — Secure DNS включается прямо в настройках ОС ([инструкция](https://remontka.pro/dns-over-https-windows-11/)). Рекомендуется, если вы пользуетесь Windows 11.
- **Роутер Keenetic** — включите опцию «Транзит запросов», иначе возможны проблемы с Secure DNS на компьютере.

### 2. Установите зависимости
Нужен Python 3.8+ для Windows. Затем:

```cmd
pip install rich requests
```

### 3. Запустите утилиту
```cmd
python main.py
```

- При запуске без прав администратора Zapretik **перезапустит себя с запросом UAC**.
- При первом запуске **автоматически скачает ядро** (`winws.exe`, `WinDivert.dll`, `WinDivert64.sys`) в папку проекта — если не работает, см. раздел «Частые вопросы».
- Убедитесь, что путь до папки не содержит кириллицу/спец. символы.

> [!NOTE]
> **Важно про `main.py`:** встроенные стратегии меню не используют пользовательские списки.
> Для обхода своих доменов (`crushon.ai` и т.п.) запускайте **bat-стратегии** — см. следующий раздел.

---

## 🚀 Bat-стратегии (основной способ)

В папке проекта лежит **22 готовые стратегии** `general*.bat` — каждая запускает `winws.exe` с нужными параметрами из `bin\`, используя листы из `lists\` и менеджер службы `service.bat`.

### Запуск
```cmd
"general (ALT9).bat"
```
Запускайте **от имени администратора**. Стратегия сама поднимает winws, перечитывает листы и пишет статус.

### Свои сайты (списки)
Хотите обходить ещё какой-то сайт — добавьте его домен (по одному на строку) в файл:

- `lists\list-general-user.txt` — сюда пишите **свои** домены (уже добавлены `crushon.ai`, `www/api/cdn.crushon.ai`, `pulsesync.dev`, `www.pulsesync.dev`);
- `lists\ipset-all.txt` — сюда можно добавить IP-адреса в формате CIDR (например `8.6.112.0/32`).

После правки **перезапустите** стратегию — списки перечитываются при каждом запуске.

### Автопубликация релизов
Новый релиз делается одной командой (обновит версии в `service.bat`/`main.py`, соберёт `.rar`, создаст тег и релиз на GitVerse, загрузит архив):
```cmd
python release.py 1.0.3
```
Файл с заметками релиза — опционально: `python release.py 1.0.3 notes.txt`.

---

## ℹ️ Меню программы

| Пункт | Стратегия | Параметры `winws.exe` |
|---|---|---|
| **1** | **Discord + YouTube (Стандарт)** — фейковая подмена TLS + фрагментация пакетов, порт TCP 443 | `--wsize=2 --split-pos=2 --fake-tls --dpi-desync=fake --filter-tcp=443` |
| **2** | **Только YouTube (Агрессивный)** — перестановка сегментов (reorder) + фрагментация, порт TCP 443 | `--wsize=1 --split-pos=1 --fake-tls --dpi-desync=reorder --filter-tcp=443` |
| **3** | **Безопасный режим (Медленный)** — только фрагментация, без фейковых пакетов | `--wsize=1 --split-pos=1 --filter-tcp=443` |
| **4** | **Остановить сервис** — корректно завершает `winws.exe` | — |
| **0** | **Выход** | — |

Во всех стратегиях дополнительно применяется `--wf-tcp=443` — перехватывается только TCP-порт 443 (меньше нагрузки на процессор).

Работоспособность стратегии зависит от многих факторов (провайдер, тип DPI). **Пробуйте разные стратегии**, пока не найдёте рабочую для вас.

### Остановка
- нажмите **Ctrl+C** в окне Zapretik — сервис остановится корректно;
- либо выберите пункт меню **4. Остановить сервис**.

### Логи и файлы
- `zapretik.log` — весь вывод `winws.exe` (создаётся в папке проекта);
- `winws.exe`, `WinDivert.dll`, `WinDivert64.sys` — ядро, скачивается автоматически;
- `bin\` — бинарники и фейк-пакеты, `lists\` — списки доменов/IP, `utils\` — служебные скрипты;
- `release.py` — автопубликация релизов.

---

## ☑️ Частые вопросы и проблемы

### Не работает Discord / YouTube
- Убедитесь, что настроен **Secure DNS** (см. выше).
- Попробуйте **другую стратегию** — например, если YouTube работает на «Стандарт», а Discord нет, попробуйте «Агрессивный» и наоборот.
- Проверьте логи `zapretik.log`: не упал ли `winws.exe` с ошибкой.

### Мой сайт не открывается, а нужно без VPN
1. Проверьте, что домен добавлен в `lists\list-general-user.txt` (по одному на строку).
2. Добавьте IP сайта (если знаете) в `lists\ipset-all.txt` в формате CIDR.
3. Перезапустите bat-стратегию от администратора и обновите страницу.
   > Zapretik обходит **DPI-блокировки** (по SNI/портам). Если сайт блокируется по IP или режется на уровне провайдера маршрутизацией — поможет только VPN/прокси.

### Обход не работает / перестал работать
- Стратегии могут переставать работать после обнаружения DPI. Это нормально — пробуйте остальные стратегии.
- Проверьте, что `winws.exe` реально запущен (в диспетчере задач).
- Убедитесь, что в логике запуска не возникло ошибок: посмотрите `zapretik.log`.
- Попробуйте полную переустановку (см. ниже).

### Как переустановить полностью?
1. Удалите из папки проекта файлы ядра: `winws.exe`, `WinDivert.dll`, `WinDivert64.sys` и `zapretik.log` (при следующем запуске они скачаются заново).
2. Убедитесь, что драйвер WinDivert не остался в службах (см. ниже).
3. Скачайте свежую версию скрипта и повторите запуск.

### Антивирус ругается на WinDivert
- Добавьте папку с Zapretik в исключения антивируса или отключите детектирование PUA (см. раздел «Антивирусы» выше).

### Античит ругается на WinDivert
- Инструкция по скрытию драйвера: https://github.com/bol-van/zapret-win-bundle/tree/master/windivert-hide

### При удалении WinDivert остаётся в службах
Узнайте название службы:
```cmd
driverquery | find "Divert"
```
Остановите и удалите её (от имени администратора):
```cmd
sc stop название_из_первого_шага
sc delete название_из_первого_шага
```

### Не запускается из-за прав
- Zapretik требует права администратора (UAC). При запуске без прав он сам перезапустится с запросом. Если UAC отключён или запрос отклонён — запустите `python main.py` от имени администратора вручную.

---

## ❤️ Поддержка

Zapretik — обёртка над проектом [bol-van/zapret](https://github.com/bol-van/zapret). Вы можете материально поддержать оригинального разработчика zapret: [ссылка на поддержку](https://github.com/bol-van/zapret?tab=readme-ov-file#поддержать-разработчика).

---

## 📜 Лицензия

Проект распространяется под лицензией **MIT**.
