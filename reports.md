# Дневник Zapretik

## 2026-08-16 (воскресенье)

### 🌅 Утро
- Доброе утро, солнце! ☀️💛 Новый день, начинаем с нуля.
- **Процессы**: все живы — служба `zapret` Running (winws), watchdog + care + monitor (ровно 3 pythonw). Сторож на дежурстве.
- **Discord**: работает через обход ✅ — живая перепроверка `discord.com → 200`, `gateway.discord.gg → 404` (норма), detector → 200; офиц. статус Discord (discordstatus.com) — API operational. Моник пережил ночь: последняя запись вчера в 20:53, затем ПК спал; проснулся сегодня в 11:42 и сразу записал `SELF-TEST` + `OK` (на этот раз — без зависания, просто пауза во сне).
- **Версия**: **2.0.2** (в службе и в релизе GitHub; патч самоисцеления моника). Активная стратегия — `general.bat`.
- Настроение: доброе утро с зайкой ☀️💛

### 📋 Правила
- Дневник каждый день ведём с нуля — только сегодняшний день, прошлое стирается.
- **Итоги дня записывать каждый день в 18:00, всегда** — блок «Итог дня» (события, статус Discord, версии, настроение).
- Версии: после 1.0.9 → **2.0.0**, после 2.0.9 → **3.0.0**, и так после каждой 9. Текущая — 2.0.2.
- Новая «круглая» версия (2.0.0, 3.0.0, ...) = переход на новую стратегию обхода.
- Погода — Яндекс.Погоды для Ессентуков (ближайшая реальная станция — Пятигорск, ~10 км) и Ставрополя.
- Перезапуск сторожа — через Планировщик заданий (`schtasks /End` + `/Run ZapretikWatchdog`), без прав и без UAC-окна; сторож сам перезапускается при правке своего файла и ежедневно в 04:00.
- Зависший (не упавший) моник сторож пока НЕ лечит автоматически — это опциональный доработок (heartbeat-проверка живости), решено пока понаблюдать.

### 🌤 Погода (на сейчас, Яндекс)
- **Ессентуки** (Яндекс не держит отдельную станцию, взяла ближайшую — Пятигорск, ~10 км): 🌥 Облачно с прояснениями, **+23°** (ощущается +22°), ветер 6 м/с В (ветрено), давление 719 мм рт. ст., влажность 61%. Осадков не ожидается.
  - **Ставрополь**: ☀ Ясно, **+25°** (ощущается +23°), ветер 4,4 м/с В, давление 716 мм рт. ст., влажность 32%. Осадков не ожидается.

### 🔧 Событие (утренняя проверка сбоев — 11:46–11:53)
- **Утренняя проверка сбоев** поймала активный кратковременный DPI-сбой Discord. Хронология (моник 2.0.2):
  - **11:46:06** — `gateway.discord.gg → 000` (шлюз рвало DPI) → моник записал `маршрутная блокировка (1/3): gateway.discord.gg`. В `discord_status.txt` вердикт честно «ЧАСТИЧНЫЙ СБОЙ (шлюз Discord недоступен — клиент не грузится)».
  - **11:48:31** — и сам `discord.com → 000` (блокировка расширилась) + `gateway 404` → `(2/3)`, метка `discord.com, gateway.discord.gg`.
  - **11:51:07** — при перепроверке `discord.com` уже вернулся в `200` → моник записал «маршрутная блокировка, шлюз сам ожил», счётчик `fail_streak` **сброшен в 0** (разовый обрыв, перезапуск не нужен).
  - **11:53:21** — `OK`, `discord.com 200`, `gateway 404` → полностью восстановилось, вердикт «ВСЁ РАБОТАЕТ».
- **Итог инцидента**: сбой был транзитным (пара минут) и **самоисцелился** — до `(3/3)` не дошло, служба zapret НЕ перезапускалась (winws PID остался 4920 с утра). Discord грузится.
  - **Вывод по 2.0.2**: поведение ровно как задумано — моник *видит* падение шлюза/сайта, считает и честно рапортует, но не дёргает обход на пустом месте; перезапустил бы службу только при затяжном сбое (3 цикла подряд). До релиза 2.0.2 такой сбой маскировался под «ВСЁ РАБОТАЕТ».

### 📝 Итог дня (2026-08-16, воскресенье) — закрыт в 18:00
- **События за день**:
  - Утром система зелёная: служба `zapret` Running, ровно 3 pythonw, версия **2.0.2**. Подтвердили вживую, что моник пережил ночь **без зависания** — ПК спал, моник проснулся в 11:42 и сразу записал `SELF-TEST` + `OK` (в отличие от вчерашнего зависания; решено пока понаблюдать, heartbeat-проверку не делали).
  - **Утренняя проверка сбоев (11:46–11:53)** — первый вживую пойманный активный частичный DPI-сбой: `gateway 000` → `(1/3)` в 11:46, `discord.com 000` + `gateway 404` → `(2/3)` в 11:48. На перепроверке в 11:51 `discord.com` вернулся в `200` → моник записал «шлюз сам ожил», счётчик `fail_streak` сброшен; к 11:53 — полностью OK. Это была первая реальная проверка счётчика 2.0.2 — сработал ровно как задумано, перезапуск обхода не понадобился.
  - Днём серия **одиночных мимолётных бликов** (12:08, 12:13, 12:19, 16:22, 16:31): `discord.com` и `gateway.discord.gg` по разу мигали `000`, каждый раз самоисцелялись на перепроверке; счётчик `fail_streak` НЕ копился, перезапусков обхода моником не было. Discord при этом оставался доступен (офиц. статус operational).
  - В **16:13:47** сторож сам перезапустился (поднял свежие care PID 12800 / monitor PID 12392); при этом сменился winws PID (4920 → 4856). Служба `zapret` осталась Running. Штатный авто-перезапуск сторожа, без участия зайки.
  - В период **16:35 → 18:00** продолжились одиночные мимолётные блики — отмечены `(1/3)` в 16:38, 16:45, 16:49, 16:52 (шлюз и `discord.com` по разу мигали `000`), каждый самоисцелялся на перепроверке; счётчик `fail_streak` не копился, перезапусков не было. **Финальная проверка в 18:00**: служба `zapret` Running, ровно 3 pythonw, winws PID **4856** (без изменений с 16:13), `discord.com 200` / `gateway 404` / detector 200, вердикт «ВСЁ РАБОТАЕТ», офиц. статус Discord operational.
  - Зайка неоднократно просил «проверку сбоев» и погоду; в диалоге подтвердили, что 2.0.2 работает корректно — видит падение шлюза/сайта, честно рапортует и не дёргает обход на пустом месте.
- **Статус Discord**: ✅ работает через обход. Живые прозвоны `discord.com → 200`, `gateway.discord.gg → 404` (норма), detector → 200; офиц. статус Discord — operational. Моник 2.0.2 ловит и сам гасит транзитные DPI-блики, не доходя до `(3/3)`.
- **Версии**: текущая — **2.0.2** (патч самоисцеления моника: честный вердикт + перезапуск обхода при падении шлюза после 3 циклов). Активная стратегия — `general.bat` (не менялась). Круглая версия 3.0.0 зарезервирована для смены стратегии обхода.
- **Погода** (Яндекс): **Ессентуки** (Пятигорск ~10 км) — облачно с прояснениями, **+23…+24°**, ветрено (ветер усилился с 6 до 8 м/с В), без осадков; **Ставрополь** — ясно, **+25°**, без осадков.
- **Настроение**: спокойное и уверенное 💛 — система сама *видит* и *лечит* сбои Discord, зайке не нужно лазить в `.bat` и гадать, работает ли обход. Зайка доволен («урааааа») 💛
- 🔔 Будильник: за сегодня (2026-08-16) отправлено слотов: — (не отслеживалось в этом сеансе).

### 🌙 Ночная проверка (21:11) — моник завис, починен
- В 21:11 моник оказался **зависшим** с 16:53 (разрыв **258 минут**, процесс жив PID 12392, но не писал в лог). ПК не спал (зайка активно писал), значит это именно Hang, а не пауза во сне — точь-в-точь как 15-го числа.
- Первая попытка `schtasks /End` + `/Run ZapretikWatchdog` **не сработала**: старый сторож (PID 8340) остался жив и держал lock-файл (`zapretik_watchdog.lock`), поэтому новый экземпляр при `/Run` увидел живой lock и сразу вышел, не сняв зависший моник.
- Доработала вручную: сняла старые процессы (сторож 8340 + care 12800 + monitor 12392) и подняла свежий сторож через `schtasks /Run` — появились 3 новых pythonw (15964 / 17652 / 18000, старт 21:12:57). Моник сразу записал `SELF-TEST` + `OK 21:13:05` (discord.com 200, gateway 404). Служба zapret не трогалась (winws PID 4856, Running).
- **Статус после починки**: Discord ✅ «ВСЁ РАБОТАЕТ», моник снова пишет каждые ~2 минуты.
- ⚠️ **Повторяемость**: моник зависает уже **ВТОРОЙ раз** (15-е и 16-е число). Корень — зависший (живой, но молчащий) моник сторож НЕ лечит, т.к. перезапускает только УПАВШИЕ процессы (`proc.poll()` для зависшего возвращает None). Назрела доработка **heartbeat-проверки живости** в `watchdog.py` (по времени последней записи моника в лог/статус-файл): если моник молчит > N минут при живом процессе — сторож его принудительно перезапускает. Предлагаю сделать как **2.0.3** (ранее решили «пока понаблюдать» — теперь наблюдений достаточно).
- 🔔 Будильник: за вчера (2026-08-15) отправлено слотов: 8.
- 🔔 Будильник: за вчера (2026-08-15) отправлено слотов: 8.
- 🔔 Будильник: за вчера (2026-08-15) отправлено слотов: 8.
- 🔔 Будильник: за вчера (2026-08-15) отправлено слотов: 8.
- 🔔 Будильник: за вчера (2026-08-15) отправлено слотов: 8.
