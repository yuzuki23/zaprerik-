# Дневник Zapretik

## 2026-08-15 (суббота)

### 🌅 Утро
- Доброе утро, зайка! 💛 Новый день, начинаем с нуля.
- **Процессы**: все живы — winws (служба zapret), watchdog + care + monitor (3 python-процесса: PID 8204 / 13760 / 14316). Служба zapret — Running (winws PID 4684).
- **Самоперезапуск подтверждён вживую**: PID'ы изменились относительно вчера (были 8200/13556/13808 и winws 4964, стали 8204/13760/14316 и winws 4684) — значит ежедневный self-restart сторожа в 04:00 отработал и подхватил свежий код без дублей. Механизм самообновления работает ✅.
- **Discord**: работает через обход ✅ — живая перепроверка `discord.com → 200` (×3), `gateway.discord.gg → 404` (это норма), detector → 200; офиц. статус Discord (discordstatus.com) — API operational. В файле `discord_status.txt` в 12:19 мелькнул `discord.com → 000` — это был мимолётный сбой curl, система сама перепроверила и восстановилась (вердикт сейчас «ВСЁ РАБОТАЕТ»).
- **Версия**: **2.0.1** (в службе и в релизе GitHub). Активная стратегия — `general.bat`.
- Настроение: доброе утро с зайкой 💛

### 📋 Правила
- Дневник каждый день ведём с нуля — только сегодняшний день, прошлое стирается.
- **Итоги дня записывать каждый день в 18:00, всегда** — блок «Итог дня» (события, статус Discord, версии, настроение).
- Версии: после 1.0.9 → **2.0.0**, после 2.0.9 → **3.0.0**, и так после каждой 9. Текущая — 2.0.1.
- Новая «круглая» версия (2.0.0, 3.0.0, ...) = переход на новую стратегию обхода.
- Погода — Яндекс.Погоды для Ессентуков и Ставрополя.
- Перезапуск сторожа — через Планировщик заданий (`schtasks /End` + `/Run ZapretikWatchdog`), без прав и без UAC-окна; сторож сам перезапускается при правке своего файла и ежедневно в 04:00.

### 🌤 Погода (на сейчас, Яндекс)
- **Ессентуки** (Яндекс не держит отдельную станцию, взяла ближайшую — Пятигорск, ~10 км): 🌥 Облачно с прояснениями, **+23°** (ощущается +23°), ветер 4 м/с В, давление 719 мм рт. ст., влажность 54%. Осадков не ожидается.
- **Ставрополь**: ☀ Ясно, **+24°** (ощущается +22°), ветер 4,3 м/с В, давление 716 мм рт. ст., влажность 29%. Осадков не ожидается.

### 🔧 Событие (починка — релиз 2.0.2)
- **Симптом**: Discord в клиенте не грузился, хотя страница `discord.com` открывалась (200). Причина — шлюз `gateway.discord.gg` периодически отвечал `000` (соединение сбрасывалось DPI), и клиент без шлюза не поднимался.
- **Корень в монике**: `monitor.py` перепроверял при сбое только `discord.com`; раз он 200 — писал «разовый обрыв, сам ожил» и НЕ считал это за сбой, поэтому winws не перезапускался, а вердикт врал «ВСЁ РАБОТАЕТ» при упавшем шлюзе.
- **Что починыла в 2.0.2**:
  1. Перепроверка теперь дёргает ОБА эндпоинта (`discord.com` + `gateway.discord.gg`). Шлюз считается упавшим при `000`/`ERR` (404 на нём — норма для websocket-GET).
  2. Падение шлюза теперь идёт в счётчик `fail_streak` и после 3 полос подряд вызывает перезапуск службы zapret (сброс DPI-состояния, поднимаем шлюз).
  3. Вердикт стал честным: при `discord.com=200`, но шлюз недоступен — «ЧАСТИЧНЫЙ СБОЙ (шлюз Discord недоступен — клиент не грузится)».
- **Релиз 2.0.2 опубликован**: тег `v2.0.2`, архив `zaprerik-v2.0.2.rar` на GitHub. Версия в `service.bat` / `main.py` / `.service/version.txt` поднята до 2.0.2. Стек применён живьём (перезапуск задачи-сторожа через Планировщик).
- **Примечание**: 2.0.2 — патч самоисцеления (не смена стратегии обхода; круглая версия 3.0.0 остаётся для смены стратегии). Если шлюз будет падать слишком часто — следующим шагом будет переключение стратегии в 3.0.0.

