# Дневник Zapretik

## 2026-08-14 (пятница)

### 🌅 Утро
- Доброе утро, зайка! 💛 Новый день, начинаем с нуля.
- **Процессы**: все живы — winws (служба zapret), watchdog + care + monitor (3 python-процесса: PID 8396 / 13208 / 7968). Служба zapret — Running.
- **Discord**: работает через обход ✅ — `discord_status.txt` (обновлено 12:31) → `discord.com 200`, detector `204`. Живая перепроверка: `discord.com → 200`, `gstatic → 204`.
- **Версия**: **2.0.1** (в службе, в релизе GitHub — патч-релиз с исправлениями). Активная стратегия — `general.bat`.
- Настроение: доброе утро с зайкой 💛

### 📋 Правила
- Дневник каждый день ведём с нуля — только сегодняшний день, прошлое стирается.
- **Итоги дня записывать каждый день в 18:00, всегда** — блок «Итог дня» (события, статус Discord, версии, настроение).
- Версии: после 1.0.9 → **2.0.0**, после 2.0.9 → **3.0.0**, и так после каждой 9. Текущая — 2.0.0.
- Новая «круглая» версия (2.0.0, 3.0.0, ...) = переход на новую стратегию обхода.
- Погода — Яндекс.Погоды для Ессентуков и Ставрополя.

### 🔧 Событие (середина дня)
- **Проблема**: Discord перестал грузиться (discord.com → «удалённый хост принудительно разорвал соединение»). Причина оказалась НЕ в «старом ядре 1.10.0», а в том, что **служба `zapret` отсутствовала / не была запущена** (winws.exe не работал, монитор крутил восстановление вхолостую, т.к. службы нет).
- **Проверка гипотезы про 1.10.1**: скачала сборку Flowsil `zapret-discord-youtube-1.10.1`. Хэши `winws.exe` / `WinDivert.dll` / `WinDivert64.sys` / `cygwin1.dll` и всех фильтров, что юзает `general.bat`, **полностью совпадают с 1.10.0** → апдейт до 1.10.1 был бы пустышкой, движок не меняется.
- **Решение**: пересоздала службу из `general.bat` через `reinstall_service.py` (батник `zapret_reinstall_service.bat` зайка запустил от админа). Служба `zapret` → **RUNNING**, winws.exe PID 2544.
- **Результат**: Discord снова работает ✅ — `discord.com → 200`, `gstatic → 204`.
- Версия ядра фактически осталась **1.10.0** (= 1.10.1 по бинарникам). Если ДС снова начнёт падать — брать реально свежее ядро (апстрим bol-van/zapret, версия >1.10.x).
